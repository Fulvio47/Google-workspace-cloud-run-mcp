{
  "name": "google-workspace-mcp",
  "version": "1.0.0",
  "description": "Master Google Workspace MCP Agent on Cloud Run",
  "type": "module",
  "main": "mcp-server.js",
  "scripts": {
    "start": "node mcp-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "express": "^4.19.0",
    "googleapis": "^144.0.0",
    "zod": "^3.23.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
